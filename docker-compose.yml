services:
  collector:
    build: ./collector
    restart: unless-stopped
    volumes:
      - ./data:/data
    env_file: .env
    environment:
      DATA_DIR: /data
      ADMIN_PORT: 8081
    ports:
      - "8081:8081"   # admin UI

  processor:
    build: ./processor
    restart: unless-stopped
    volumes:
      - ./data:/data
    env_file: .env
    environment:
      DATA_DIR: /data
    ports:
      - "8080:8080"   # manual trigger endpoint
